

<li class="nav-item dropdown border-left">
    <a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
        <i class="mdi mdi-email"></i>
        <span class="count bg-success"></span>
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
        <h6 class="p-3 mb-0">Son Mesajlar</h6>
        <div class="dropdown-divider"></div>
        @foreach (var item in Model)
        {
            <a href="/AdminMesasge/AdminMessageDetails/@item.WriterMessageID" class="dropdown-item preview-item">
                <div class="preview-thumbnail">
                    <img src="~/userimage/@ViewBag.senderImage" alt="image" class="rounded-circle profile-pic">
                </div>
                <div class="preview-item-content">
                    <p class="preview-subject ellipsis mb-1">@item.SenderName sana bir mesaj gönderi.</p>
                    <p class="text-muted mb-0"> @(((DateTime)item.Date).ToString("dd MMM yyyy")) </p>
                </div>
            </a>
            <div class="dropdown-divider"></div>
        }
        <p class="p-3 mb-0 text-center"><a href="">54 Yeni Mesaj</a></p>
    </div>
</li>
